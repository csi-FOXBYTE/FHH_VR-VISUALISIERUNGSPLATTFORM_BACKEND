import {
  createService,
  InferService,
  ServiceContainer,
} from "@csi-foxbyte/fastify-toab";
import dayjs from "dayjs";
import { getAuthService } from "../auth/auth.service.js";
import { getDbService } from "../db/db.service.js";

const eventsService = createService("events", async ({ services }) => {
  const HEARTBEAT_DELAY_MS = 15_000;
  const HEARTBEAT_CHECK_MS = 20_000;

  const dbService = await getDbService(services);
  const authService = await getAuthService(services);

  async function fetchAll() {
    const enhancedClient = await dbService.getEnhancedClient();

    return await enhancedClient.event.findMany({
      select: {
        title: true,
        status: true,
        joinCode: true,
      },
    });
  }

  async function fetchStatus(id: string) {
    const enhancedClient = await dbService.getEnhancedClient();

    return await enhancedClient.event.findFirstOrThrow({
      where: {
        id,
      },
      select: {
        joinCode: true,
        status: true,
      },
    });
  }

  async function checkHeartbeat(id: string) {
    const enhancedClient = await dbService.getEnhancedClient();

    const { heartbeatTimestamp, status } =
      await enhancedClient.event.findFirstOrThrow({
        where: { id },
        select: { heartbeatTimestamp: true, status: true },
      });

    if (status !== "ACTIVE") return;

    if (
      dayjs(heartbeatTimestamp).isBefore(
        dayjs().subtract(HEARTBEAT_DELAY_MS, "second")
      )
    )
      return;

    await enhancedClient.event.update({
      where: {
        id,
      },
      data: {
        status: "MISSING_HOST",
      },
    });
  }

  return {
    async createEvent({
      attendees,
      endTime,
      startTime,
      status,
      title,
    }: {
      endTime: string;
      startTime: string;
      status: string;
      title: string;
      attendees: string[];
    }) {
      const enhancedClient = await dbService.getEnhancedClient();

      await enhancedClient.event.create({
        data: {
          endTime,
          startTime,
          status,
          title,
          owner: {
            connect: {
              email: (await authService.getSession())!.user.email,
            },
          },
          attendees: {
            createMany: {
              data: attendees.map((attendee) => ({ userId: attendee })),
            },
          },
        },
      });
    },

    async updateEvent({
      id,
      attendees,
      endTime,
      startTime,
      status,
      title,
    }: {
      id: string;
      endTime?: string;
      startTime?: string;
      status?: string;
      title?: string;
      attendees?: string[];
    }) {
      const enhancedClient = await dbService.getEnhancedClient();

      return await enhancedClient.event.update({
        data: {
          endTime,
          startTime,
          status,
          title,
          attendees: attendees
            ? {
                deleteMany: {},
                createMany: {
                  data: attendees.map((attendee) => ({ userId: attendee })),
                },
              }
            : undefined,
        },
        where: {
          id,
        },
      });
    },

    fetchAll,

    fetchStatus,

    list() {
      return dbService.rawClient.event.subscribe({ operations: ["*"] });
    },

    status(id: string) {
      return dbService.rawClient.event.subscribe({ operations: ["*"] });
    },

    async setHeartbeat(id: string) {
      const enhancedClient = await dbService.getEnhancedClient();

      await enhancedClient.event.update({
        where: {
          id,
        },
        data: {
          heartbeatTimestamp: new Date().toISOString(),
        },
      });

      setTimeout(() => checkHeartbeat(id), HEARTBEAT_CHECK_MS);
    },

    checkHeartbeat,

    async hostSession(id: string, joinCode: string) {
      const enhancedClient = await dbService.getEnhancedClient();

      await enhancedClient.event.update({
        where: {
          id,
        },
        data: {
          status: "ACTIVE",
          heartbeatTimestamp: new Date().toISOString(),
          joinCode,
        },
      });

      setTimeout(() => checkHeartbeat(id), HEARTBEAT_CHECK_MS);
    },

    async endSession(id: string) {
      const enhancedClient = await dbService.getEnhancedClient();

      await enhancedClient.event.update({
        where: {
          id,
        },
        data: {
          status: "END",
          joinCode: null,
          heartbeatTimestamp: null,
        },
      });
    },

    async rehostSession(id: string, joinCode: string) {
      const enhancedClient = await dbService.getEnhancedClient();

      await enhancedClient.event.update({
        where: {
          id,
        },
        data: {
          status: "ACTIVE",
          joinCode,
          heartbeatTimestamp: new Date().toISOString(),
        },
      });
    },
  };
});

/*
AUTOGENERATED!
*/

export { eventsService };
export type EventsService = InferService<typeof eventsService>;
export function getEventsService(deps: ServiceContainer) {
  return deps.get<EventsService>(eventsService.name);
}
